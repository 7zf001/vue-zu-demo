export const commonParams = {
  
}

export const options = {
  
}

export const ERR_OK = 0

export const host = process.env.NODE_ENV !== 'production' ? 'http://test.lewo/' : 'https://mp.loveto.co/'

export const apiPath = 'admin/api/'